/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  // basePath: "/website",
  // assetPrefix: "/website",
  // distDir: "docs",
  trailingSlash: true,
};

export default nextConfig;
